import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  ImageContainer: {
    alignItems: "center",
  },
  tinyLogo: {
    width: 200,
    height: 200,
    borderRadius: 100,
  },
});

export { styles };
